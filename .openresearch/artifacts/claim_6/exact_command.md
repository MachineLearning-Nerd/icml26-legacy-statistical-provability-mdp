# Exact command and compute contract

```bash
uv sync --frozen && uv run --frozen python -m reproduction.run_all
```

The proof certificate is seed-free. It requires one CPU core and less than one
minute after environment setup. Exact versions are pinned in `uv.lock`.
